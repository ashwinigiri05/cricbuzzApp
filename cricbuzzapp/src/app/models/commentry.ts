export class Commentry{
    commentry:string;
    run:number;
    over:number;

    constructor(commentry:string,run:number,over:number){
        this.commentry=commentry;
        this.run=run;
        this.over=over
    }
}